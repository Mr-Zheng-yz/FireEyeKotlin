<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.baize.fireeyekotlin">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".app.FireEyeApp"
        android:allowBackup="false"
        android:icon="@mipmap/icon_tv_lancher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_tv_lancher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:allowBackup">
        <activity
            android:name=".ui.TransitionActivity"
            android:theme="@style/ViewBigPicTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.MainActivity" />
        <activity
            android:name=".ui.VideoDetailActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:screenOrientation="portrait"
            android:theme="@style/VideoBlackTheme" />
        <activity android:name=".ui.ResultActivity" />
        <activity android:name=".ui.RankDetailActivity" />
        <activity android:name=".ui.AdviseActivity"></activity>
    </application>

</manifest>